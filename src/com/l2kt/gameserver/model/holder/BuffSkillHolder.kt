package com.l2kt.gameserver.model.holder

/**
 * A container used for schemes buffer.
 */
class BuffSkillHolder(id: Int, price: Int, val type: String, val description: String) : IntIntHolder(id, price)